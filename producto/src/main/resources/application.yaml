spring:
  application:
    name: producto

  datasource:
    url: jdbc:postgresql://localhost:5432/name_db
    username: #
    password: #
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
    show-sql: true